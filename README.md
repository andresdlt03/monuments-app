Primero tendremos que descargar las librerias usando el comando:

```bash
pip install -r "requirements.txt"
```

Despues para iniciar el proceso tendremos que iniciarlo con el comando:

```bash
python main.py
```